server:
  port: 0

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:postgresql://localhost:5440/user_db
    username: user_service
    password: user_password

logging:
  level:
    root: INFO
    ru.practicum: DEBUG